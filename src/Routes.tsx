import { BrowserRouter, Routes, Route } from "react-router-dom";

export const AppRouters = () => {
  return (
    <>
      <BrowserRouter>
        <Routes>
          
        </Routes>
      </BrowserRouter>
    </>
  );
};
